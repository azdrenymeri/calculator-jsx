import operate from './operate';

const calculate = (calculatorObj, buttonName) => {
    
    if(buttonName === 'AC') {
        return {
            total:0,
            next:0,
            operation:null
        };
    }
    
}

export default calculate;